// Ouedna app home: the web counterpart of the Flutter HomePage, not a marketing-only landing page.
import { createClient } from "@supabase/supabase-js";
import { ArrowLeft, CalendarDays, Compass, Heart, Info, MapPinned, Navigation, Plus, ShieldAlert, Sparkles, Star, Users } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import PlatformFrame from "@/components/platform/PlatformFrame";
import { SUPABASE_ANON_KEY, SUPABASE_URL } from "@/lib/supabase/config";

type HomePlace = { id: number; name: string; description: string | null; category: string | null; municipality: string | null; image: string; rating: number };

function getFirstImage(value: unknown) {
  if (Array.isArray(value)) return String(value[0] || "");
  if (typeof value === "string") return value.replace(/[\[\]"']/g, "").split(",")[0]?.trim() || "";
  return "";
}

async function getHomePlaces(): Promise<HomePlace[]> {
  const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
  const { data } = await supabase.from("places").select("*").eq("status", "منشور").order("id", { ascending: false }).limit(6);
  return (data || []).map((place) => ({ id: place.id, name: place.name, description: place.description, category: place.category || place.main_category, municipality: place.municipality, image: getFirstImage(place.image_url) || "/ouedna/local-architecture.webp", rating: Number(place.rating || 0) })).filter((place) => place.name);
}

export const dynamic = "force-dynamic";

export default async function HomePage() {
  const places = await getHomePlaces();
  return <PlatformFrame active="/"><div className="app-home"><div className="app-home__container"><section className="app-home__top"><div><span className="platform-eyebrow"><i />الدليل السياحي لوادي سوف</span><h1>ابدأ رحلتك<br /><em>من هنا.</em></h1></div><p>كل ما تحتاجه لاكتشاف المعالم، تخطيط المسار، وحفظ حكايات المكان في تجربة واحدة تشبه تطبيق Ouedna.</p></section><section className="app-home__hero"><Image src="/ouedna/hero-oasis.jpg" alt="واحة وادي سوف" fill priority sizes="(max-width: 900px) 100vw, 1180px" /><div className="app-home__hero-stamp"><strong>360°</strong>واحة، قبة<br />وذاكرة</div><div className="app-home__hero-content"><span className="platform-eyebrow platform-eyebrow--light"><i />مرحباً بك في وادنا</span><h2>اكتشف الوادي<br />على إيقاعك.</h2><p>استكشف المعالم القريبة، احفظ ما يعجبك، ثم افتح الخريطة لبدء طريقك.</p><div className="app-home__hero-actions"><Link className="platform-button platform-button--amber" href="/explore"><Compass size={17} /> استكشف المعالم</Link><Link className="platform-button platform-button--outline" href="/map"><MapPinned size={17} /> افتح الخريطة</Link></div></div></section><section className="app-action-grid" aria-label="اختصارات التطبيق"><Link className="app-action-card" href="/explore"><span className="app-action-card__icon"><Compass size={20} /></span><span><h3>استكشف الأماكن</h3><p>معالم قريبة وصور حقيقية.</p></span></Link><Link className="app-action-card" href="/map"><span className="app-action-card__icon"><MapPinned size={20} /></span><span><h3>الخريطة والمسار</h3><p>خطط الطريق وتتبع موقعك.</p></span></Link><Link className="app-action-card" href="/itinerary"><span className="app-action-card__icon"><CalendarDays size={20} /></span><span><h3>بوصلة الرحلة</h3><p>برنامج سريع أو يوم كامل.</p></span></Link><Link className="app-action-card" href="/guide"><span className="app-action-card__icon"><Sparkles size={20} /></span><span><h3>المساعد السياحي</h3><p>اسأل عن المكان والرحلة.</p></span></Link><Link className="app-action-card" href="/suggest-place"><span className="app-action-card__icon"><Plus size={20} /></span><span><h3>اقترح معلماً</h3><p>أضف مكاناً للمراجعة.</p></span></Link><Link className="app-action-card" href="/favorites"><span className="app-action-card__icon"><Heart size={20} /></span><span><h3>مفضلتي</h3><p>أماكن تحفظها للرحلة.</p></span></Link></section><section><div className="app-section-head"><div><span className="platform-eyebrow"><i />اختيارات من الدليل</span><h2>أماكن تستحق<br />أن تراها.</h2></div><p>المعالم المنشورة من قاعدة Ouedna الحية. افتح التفاصيل لمعرفة الموقع والصور وبدء الرحلة.</p></div>{places.length ? <div className="app-places-grid">{places.slice(0, 3).map((place) => <article className="app-place-card" key={place.id}><Link href={`/place/${place.id}`} className="app-place-card__image"><Image src={place.image} alt={place.name} fill sizes="(max-width: 520px) 100vw, 33vw" /><span className="app-place-card__tag">{place.category || "معلم سياحي"}</span></Link><div className="app-place-card__body"><h3>{place.name}</h3><p>{place.description || "تفاصيل محلية من ولاية الوادي."}</p><div className="app-place-card__meta"><span><Star size={13} fill="currentColor" /> {place.rating ? place.rating.toFixed(1) : "جديد"} · {place.municipality || "الوادي"}</span><Link href={`/place/${place.id}`}>التفاصيل <ArrowLeft size={13} /></Link></div></div></article>)}</div> : <div className="platform-empty-panel"><Compass size={28} /><h2>المعالم قيد التحميل</h2><p>افتح الاستكشاف لإعادة المحاولة عند توفر الاتصال.</p></div>}</section><div className="app-live-note"><Info size={22} /><p><strong>بيانات حية من Ouedna:</strong> المعالم والصور والتحديثات تأتي من Supabase وتظهر بعد اعتمادها من الإدارة، لتبقى تجربة الكمبيوتر والهاتف متزامنة.</p></div><section className="app-utility-grid"><article className="app-utility-card"><div><h3>خطّط يومك في سوف</h3><p>اجمع الأماكن المفضلة وافتح الخريطة لتبدأ المسار.</p></div><div className="app-utility-card__actions"><Link className="app-link-button" href="/itinerary"><Navigation size={15} /> مخطط الرحلة</Link><Link className="app-link-button" href="/community"><Users size={15} /> صوت الزوار</Link></div></article><article className="app-utility-card app-utility-card--sand"><div><h3><ShieldAlert size={17} /> أرقام النجدة</h3><p>الوصول السريع عند الحاجة.</p></div><div className="app-utility-card__actions"><a className="app-call-link" href="tel:1055">شرطة 1055</a><a className="app-call-link" href="tel:1548">درك 1548</a></div></article></section><section className="app-utility-grid"><Link className="app-utility-card app-utility-card--sand" href="/archive"><div><h3><Sparkles size={17} /> ذاكرة الوادي</h3><p>صور قديمة وحكايات من المكان.</p></div><ArrowLeft size={19} /></Link><Link className="app-utility-card" href="/community"><div><h3><Users size={17} /> شارك تجربتك</h3><p>صورة، ملاحظة، اقتراح أو سؤال.</p></div><ArrowLeft size={19} /></Link></section></div></div></PlatformFrame>;
}
